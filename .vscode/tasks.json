{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pyuic",
            "type": "shell",
            "command": "pyuic5 ${file} -o ${workspaceFolder}\\windo\\ui\\${fileBasenameNoExtension}.py"
        },
        {
            "label": "pyrcc",
            "type": "shell",
            "command": "pyrcc5 ${file} -o ${workspaceFolder}\\windo\\ui\\${fileBasenameNoExtension}_rc.py",
        }
    ]
}